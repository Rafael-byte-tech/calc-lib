/**
 * calc-lib/src/index.ts
 * author´s question: how make jest tests for this. Including the coverage.
 */

export const greet = (name: string): string => {
    return "Hellow, " + name + '...welcome...';
};